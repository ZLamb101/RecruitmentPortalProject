<?php 
session_start();
include "header.phtml"
 ?>
    <script type = "text/javascript" src="src/js/populateSubFields.js"></script>
    <script type = "text/javascript" src="src/js/livesearch.js"></script>
    <script type = "text/javascript" src="src/js/updateShortList.js"></script>

	<!--- Content Start -->
	<div class= "page from-top">


        <div id="search-params">
            <form>
                <p>All Categories</p>
                <select id="field0" name="field0" onclick="updateFields(this)">
                    <?php
                    echo "<option value=\"all\">all categories</option>";
                    foreach ($Fields as $field){
                        echo "<option value=\"".$field['id']."\">".$field['field']."</option>";
                    }
                    ?>
                </select>

                <p>All Sub-Categories</p>
                <select id ="sub-field0" name="sub-field0">
                    <option value="all">All Sub-Categories</option>
                </select>
                <input type="hidden" value="0" name="skill-count" id="skill-count">

                <div id="qualification" name="qualification">
                    <p>Qualification</p>
                    <select id="qual0" name="qual0" >
                        <option value="all">All Qualifications</option>
                        <?php
                        foreach ($Quals as $qual) {
                            echo "<option value=\"".$qual['id']."\">".$qual['type']."</option>";
                        }
                        ?>
                    </select>
                </div>

                <div id="availability" name="availability">
                    <p>Availability</p>
                    <input type="checkbox" value="fullTime" name="full-time" id="full-time" checked>
                    <label for="fullTime">Full Time</label>
                    <input type="checkbox" value="partTime" name="part-time" id="part-time" checked>
                    <label for="partTime">Part Time</label>
                    <input type="checkbox" value="casual" name="casual" id="casual" checked>
                    <label for="casual">Casual</label>
                    <input type="checkbox" value="contractor" name="contractor" id="contractor" checked>
                    <label for="contractor">Contractor</label>
                </div>


                <div id="skills-search-bar">
    				<p> Search Skills:  <input id="skill-search" type="text" size="50"></p>
    			</div>

                <input id="submit-search" type="button" value="Search" onclick="showResult()">
            </form>
        </div>
		<br>

		<div id = "search">
        	<p><b>Search Results</b></p>
        	<div id="livesearch"> </div>
        </div>
          


		<br>
        <div id ="shortlist">
            <select id="shortlist0" name="shortlist0" onclick="updateShortList(this)">
                <option value="all">All Shortlists</option>
                <?php
                    $shortLists = $employerInfo->getShortLists();
                    foreach ($shortLists as $shortList) {
                        echo "<option value=\"".$shortList->getId()."\">".$shortList->getName()."</option>";
                    }
                ?>
            </select>
            <input type="button" id="send0" name="sendInvites" value="Send Invite Email" onclick="writeEmail()">
            <div id="shortlist-candidates">
                
            </div>
        </div>
	</div>
	<!--- Content Ends here ----------->



<!---------- Body ends -------------------->
<?php include "footer.phtml" ?>